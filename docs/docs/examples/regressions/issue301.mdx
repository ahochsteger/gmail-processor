---
id: issue301
title: getBlob Issue
description: Tests a fix for `getBlob` error on conflict strategy `update`.
---
import RenderExample from "/src/components/RenderExample.tsx"
import config from '!!raw-loader!../../../../src/examples/regressions/issue301.json';
import script from '!!raw-loader!../../../../src/gas/examples/regressions/issue301.js';
import { info } from "../../../../src/examples/regressions/issue301.ts"

This example is a test for PR #301 to fix `getBlob` error on conflict strategy `update`.

<RenderExample info={info} config={config} script={script} />
