---
id: actionThreadRemoveLabel
title: Remove Thread Label
description: The action `thread.removeLabel` removes a label from a thread.
---
import RenderExample from "/src/components/RenderExample.tsx"
import config from '!!raw-loader!../../../../src/examples/actions/actionThreadRemoveLabel.json';
import script from '!!raw-loader!../../../../src/gas/examples/actions/actionThreadRemoveLabel.js';
import { info } from "../../../../src/examples/actions/actionThreadRemoveLabel.ts"

This example demonstrates the usage of the action `thread.removeLabel`.
It is also used to test a possible behavioral change for certain characters in label names (e.g. `-` vs. `/`).

<RenderExample info={info} config={config} script={script} />
