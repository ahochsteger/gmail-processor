name: pr

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      trigger_push_lib:
        description: "Push built PR lib code to GAS"
        type: boolean
        default: false
      trigger_push_examples:
        description: "Push built PR examples code to GAS"
        type: boolean
        default: false
      skip_build_docs:
        description: "Skip build docs"
        type: boolean
        default: false
      skip_build_lib:
        description: "Skip build lib"
        type: boolean
        default: false
      skip_qa_coveralls:
        description: "Skip QA Coveralls"
        type: boolean
        default: false
      skip_qa_sonarqube:
        description: "Skip QA SonarQube"
        type: boolean
        default: false
      skip_test:
        description: "Skip test"
        type: boolean
        default: false

jobs:
  pr-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    concurrency: ci-${{ github.ref }}
    environment:
      name: pr
    env:
      CLASP_ACCESS_TOKEN: ${{secrets.CLASP_ACCESS_TOKEN}}
      CLASP_CLIENT_ID: ${{secrets.CLASP_CLIENT_ID}}
      CLASP_CLIENT_SECRET: ${{secrets.CLASP_CLIENT_SECRET}}
      CLASP_EXAMPLES_DEPLOYMENT_ID: ${{secrets.CLASP_EXAMPLES_DEPLOYMENT_ID}}
      CLASP_EXAMPLES_SCRIPT_ID: ${{secrets.CLASP_EXAMPLES_SCRIPT_ID}}
      CLASP_ID_TOKEN: ${{secrets.CLASP_ID_TOKEN}}
      CLASP_LIB_DEPLOYMENT_ID: ${{secrets.CLASP_LIB_DEPLOYMENT_ID}}
      CLASP_LIB_SCRIPT_ID: ${{secrets.CLASP_LIB_SCRIPT_ID}}
      CLASP_REFRESH_TOKEN: ${{secrets.CLASP_REFRESH_TOKEN}}
      GCLOUD_PROJECT_ID: ${{secrets.GCLOUD_PROJECT_ID}}
      GCLOUD_PROJECT_NR: ${{secrets.GCLOUD_PROJECT_NR}}
    steps:
      - id: checkout
        name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - id: setup-nodejs
        name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "24.x"
          cache: "npm"
      - id: build
        if: ${{ !inputs.skip_build_lib || inputs.trigger_push_lib || inputs.trigger_push_examples }}
        name: Build lib and examples
        run: npm run ci:build
      - id: test
        if: ${{ !inputs.skip_test }}
        name: Test
        run: npm run ci:test
      - id: build-docs
        if: ${{ !inputs.skip_build_docs }}
        name: Build docs
        run: npm run ci:docs
      - id: qa-coveralls
        if: ${{ !inputs.skip_qa_coveralls }}
        name: QA Coveralls
        uses: coverallsapp/github-action@5cbfd81b66ca5d10c19b062c04de0199c215fb6e # v2
      - id: qa-sonarqube
        if: ${{ !inputs.skip_qa_sonarqube }}
        name: QA SonarQube
        uses: SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9 # v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - id: deploy-lib-pr
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.trigger_push_lib }}
        name: Deploy Lib (PR)
        run: |
          if [ -z "${CLASP_LIB_SCRIPT_ID}" ]; then
            echo "::warning::CLASP_LIB_SCRIPT_ID is empty. Please ensure you have added it to the 'pr' environment secrets or repository secrets. Skipping push."
            exit 0
          fi
          npm run push:lib
      - id: deploy-examples-pr
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.trigger_push_examples }}
        name: Deploy Examples (PR)
        run: |
          if [ -z "${CLASP_EXAMPLES_SCRIPT_ID}" ]; then
            echo "::warning::CLASP_EXAMPLES_SCRIPT_ID is empty. Please ensure you have added it to the 'pr' environment secrets or repository secrets. Skipping push."
            exit 0
          fi
          npm run push:examples
